# Release Notes v0.1.0

OVDS v0.1.0 is the first tagged public release of the Open Vehicle Data Standard.

This release establishes the initial stable schema set, the normative semantics for the model, the repository documentation structure, and a reproducible validation workflow using uv.

## Highlights

- Introduces a concrete vehicle record with separate lineage, configuration, origin, specs, performance, cost-of-ownership, safety, compliance, and attributes blocks.
- Defines reusable schemas for groups, makes, platforms, engines, electric motors, battery packs, transmissions, and normalized safety features.
- Uses a role-based powertrain model that separates energy sources, storage, converters, and traction motors.
- Separates published performance results from compliance classifications.
- Adds a dedicated origin block for published assembly country, region, and plant information.
- Includes cargo capacity and ground clearance in `specs`, plus acceleration, top speed, and torque in `performance`.
- Adds charging interfaces, separate AC/DC charging capability, and charging-time scenarios to the vehicle configuration model.
- Documents human-readable, semantic OVDS identifiers and clarifies that one commercial variant may map to multiple concrete vehicle records.
- Includes valid EV and hybrid reference examples plus a local validation script and an intentionally invalid payload for negative testing.

## Schema Set

The v0.1.0 release includes these primary schema files:

- [spec/v0.1/vehicle.schema.json](../../spec/v0.1/vehicle.schema.json)
- [spec/v0.1/group.schema.json](../../spec/v0.1/group.schema.json)
- [spec/v0.1/make.schema.json](../../spec/v0.1/make.schema.json)
- [spec/v0.1/platform.schema.json](../../spec/v0.1/platform.schema.json)
- [spec/v0.1/safety.schema.json](../../spec/v0.1/safety.schema.json)
- [spec/v0.1/engine.schema.json](../../spec/v0.1/engine.schema.json)
- [spec/v0.1/electric-motor.schema.json](../../spec/v0.1/electric-motor.schema.json)
- [spec/v0.1/battery-pack.schema.json](../../spec/v0.1/battery-pack.schema.json)
- [spec/v0.1/transmission.schema.json](../../spec/v0.1/transmission.schema.json)

The normative semantic rules for this release live in [spec/v0.1/readme.md](../../spec/v0.1/readme.md).

## Examples

The release includes example payloads under [examples/v0.1](../../examples/v0.1):

- [examples/v0.1/ev.json](../../examples/v0.1/ev.json): canonical fictional battery-electric reference vehicle
- [examples/v0.1/mhev.json](../../examples/v0.1/mhev.json): mild-hybrid example with combustion engine, battery, electric assist motor, and manual transmission
- [examples/v0.1/hev.json](../../examples/v0.1/hev.json): full-hybrid power-split example with combustion engine and traction motor
- [examples/v0.1/phev.json](../../examples/v0.1/phev.json): plug-in hybrid example with external charging plus combustion and electric propulsion
- [examples/v0.1/series-hybrid.json](../../examples/v0.1/series-hybrid.json): series-hybrid example where the combustion engine acts as a generator and the electric motor drives the wheels
- [examples/v0.1/invalid.json](../../examples/v0.1/invalid.json): intentionally invalid example for negative validation testing

Together, these examples show how the same vehicle model can express distinct electrification strategies while `vehicle.id` continues to identify a unique published configuration record.

## Validation

Validate the full schema set:

```bash
uv sync
uv run python scripts/validate_schemas.py
```

Validate the positive examples:

```bash
uv run python scripts/validate_schemas.py \
  --schema-id https://ovds.xyz/spec/v0.1/vehicle.schema.json \
  --instance examples/v0.1/ev.json \
  --instance examples/v0.1/mhev.json \
  --instance examples/v0.1/hev.json \
  --instance examples/v0.1/phev.json \
  --instance examples/v0.1/series-hybrid.json
```

Validate the negative example and expect failure:

```bash
uv run python scripts/validate_schemas.py \
  --schema-id https://ovds.xyz/spec/v0.1/vehicle.schema.json \
  --instance examples/v0.1/invalid.json
```

## Deferred For Future Versions

The following areas are intentionally left for later versions:

- dedicated schemas for fuel-cell stacks
- richer safety taxonomy beyond grouped booleans
- explicit schemas for model, generation, and variant entities
- fitment or application records across multiple vehicles and markets
- richer market-specific availability and packaging semantics
